Game.assetManager = (function() {
  var assets = [
    'assets/'
  ];

  var assetManager = {
    load : function(){
      // Crafty.load();
      return;
    },
  };

  return assetManager;
})(Game);